export default defineAppConfig({
  // App-level configuration for Nuxt UI
  // See: https://ui.nuxt.com/getting-started/theming
  ui: {
    colors: {
      // Primary color used throughout the app (buttons, badges, highlights)
      // Options: red, orange, amber, yellow, lime, green, emerald, teal,
      //          cyan, sky, blue, indigo, violet, purple, fuchsia, pink, rose
      primary: 'violet',
      neutral: 'slate'
    }
  }
})
